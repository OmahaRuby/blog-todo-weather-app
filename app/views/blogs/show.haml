%h1
  = @blog.title
  
%h2
  Entries
  
%table.table.table-striped
  %thead
    %tr
      %th
        Name
      %th
        Completed At
      %th
  %tbody
    - @blog.entries.each do |entry|
      %tr
        %td
          = entry.title
        %td
          = entry.completed_at
        %td
          = link_to 'View', entry_path(entry)
        